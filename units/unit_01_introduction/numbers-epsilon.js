var string = 'darth vader';
string.toUpperCase(); //DARTH VADER
string.charAt(0); // d
string.charAt(string.length-1); //returns the last char ‘r’.
string.slice(1); //arth vader
string.split(' '); //["darth", "vader"]
